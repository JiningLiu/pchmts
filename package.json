{
  "name": "pchmts",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "chmod +x ./build.sh && ./build.sh && bun build --compile --minify --sourcemap --bytecode index.ts --outfile pchmts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "mpegts.js": "^1.8.0"
  }
}
